# Contributing

1. Fork the repository and create a focused branch.
2. Do not commit credentials, cookies, account caches, logs, or generated artifacts.
3. Run `gofmt -w` on changed Go files.
4. Run `go test ./...`, `go vet ./...`, and `go build ./...` before opening a pull request.
5. Describe behavior changes and include tests where practical.
